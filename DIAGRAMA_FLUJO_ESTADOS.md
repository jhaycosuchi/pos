# 🔄 Diagrama de Flujo - Estados y Filtrados

## Flujo Visual Completo

```
┌─────────────────────────────────────────────────────────────────┐
│                      MESERO CREA PEDIDO                         │
│                  (Mesa 2 o PARA_LLEVAR)                         │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
        ┌─────────────────────────────────────┐
        │  BD: INSERT cuentas                 │
        │  estado = 'abierta' (INICIAL)       │
        │  mesa_numero = '2' o 'PARA_LLEVAR'  │
        └─────────────┬───────────────────────┘
                      │
                      ▼
    ┌──────────────────────────────────────────────┐
    │ API /areas-activas                          │
    │ WHERE estado IN ('abierta', 'cerrada')      │
    │ ✓ Devuelve (estado='abierta')               │
    └──────────────────┬───────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────────────┐
        │   Frontend Recibe (estado='abierta') │
        └──────────────┬───────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ▼              ▼              ▼
   ┌────────────┐ ┌────────────┐ ┌─────────────┐
   │ MESAS Tab │ │LLEVAR Tab  │ │ COBRAR Tab  │
   │ (Filter)  │ │  (Filter)  │ │  (Filter)   │
   └────────────┘ └────────────┘ └─────────────┘
        │              │              │
        ▼              ▼              ▼
   ┌────────────┐ ┌────────────┐ ┌─────────────┐
   │ IF mesa_num│ │ IF mesa_num│ │ IF estado = │
   │ != LLEVAR  │ │ = LLEVAR   │ │ 'cerrada'   │
   │ ✓ MOSTRAR │ │ ✓ MOSTRAR │ │ ✗ NO MOSTRAR│
   └────────────┘ └────────────┘ └─────────────┘


════════════════════════════════════════════════════════════════════


    MESERO CIERRA CUENTA (hace click "Cerrar")
                         │
                         ▼
        ┌─────────────────────────────────────┐
        │ BD: UPDATE cuentas                  │
        │ SET estado = 'cerrada'              │
        │ WHERE id = 3                        │
        └─────────────┬───────────────────────┘
                      │
                      ▼
    ┌──────────────────────────────────────────────┐
    │ API /areas-activas                          │
    │ WHERE estado IN ('abierta', 'cerrada')      │
    │ ✓ Devuelve (estado='cerrada')               │
    └──────────────────┬───────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────────────┐
        │   Frontend Recibe (estado='cerrada') │
        └──────────────┬───────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ▼              ▼              ▼
   ┌────────────┐ ┌────────────┐ ┌─────────────┐
   │ MESAS Tab │ │LLEVAR Tab  │ │ COBRAR Tab  │
   │ (Filter)  │ │  (Filter)  │ │  (Filter)   │
   └────────────┘ └────────────┘ └─────────────┘
        │              │              │
        ▼              ▼              ▼
   ┌────────────┐ ┌────────────┐ ┌─────────────┐
   │ IF estado= │ │ IF estado= │ │ IF estado = │
   │ 'abierta'  │ │ 'abierta'  │ │ 'cerrada'   │
   │ ✗ NO MUEST │ │ ✗ NO MUEST │ │ ✓ MOSTRAR  │
   └────────────┘ └────────────┘ └─────────────┘
        ✗              ✗              ✓
     BORRADA       BORRADA        APARECE


════════════════════════════════════════════════════════════════════


    CAJA COBRA CUENTA (hace click "Cobrar" + pagar)
                         │
                         ▼
        ┌─────────────────────────────────────┐
        │ BD: UPDATE cuentas                  │
        │ SET estado = 'cobrada'              │
        │ WHERE id = 3                        │
        └─────────────┬───────────────────────┘
                      │
                      ▼
    ┌──────────────────────────────────────────────┐
    │ API /areas-activas                          │
    │ WHERE estado IN ('abierta', 'cerrada')      │
    │ ✗ NO Devuelve (estado='cobrada' excluido)   │
    └──────────────────┬───────────────────────────┘
                       │
                       ▼
        ┌──────────────────────────────────────┐
        │ Frontend NO Recibe la cuenta         │
        │ (Fue eliminada del API response)     │
        └──────────────┬───────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
        ▼              ▼              ▼
   ┌────────────┐ ┌────────────┐ ┌─────────────┐
   │ MESAS Tab │ │LLEVAR Tab  │ │ COBRAR Tab  │
   │           │ │            │ │             │
   └────────────┘ └────────────┘ └─────────────┘
        ✗              ✗              ✗
     NADA          NADA           NADA
   (nunca existió, nunca existió, DESAPARECIÓ)
```

---

## Estados Válidos y Transiciones

```
┌─────────────────────────────────────────────────────┐
│              MÁQUINA DE ESTADOS                      │
└─────────────────────────────────────────────────────┘

    ┌──────────┐
    │          │
    │ ABIERTA  │◄─── Nuevo pedido se crea aquí
    │          │
    └────┬─────┘
         │
         │ Mesero cierra
         │ (puede agregar más pedidos antes)
         ▼
    ┌──────────┐
    │          │
    │ CERRADA  │◄─── Espera cobro
    │          │
    └────┬─────┘
         │
         │ Caja cobra
         ▼
    ┌──────────┐
    │          │
    │ COBRADA  │  ◄─── FINAL (no sale de aquí)
    │          │
    └──────────┘


Transiciones Válidas:
  abierta   → cerrada    (mesero cierra)
  cerrada   → cobrada    (caja cobra)
  cobrada   → (nada)     (fin de ciclo)

Transiciones Inválidas (error):
  abierta   → cobrada    ✗ (debe cerrar primero)
  cerrada   → abierta    ✗ (no se reabre)
  cobrada   → cerrada    ✗ (no vuelve atrás)
```

---

## Dónde Aparece Cada Estado

```
┌──────────────────────────────────────────────────────┐
│              VISIBILIDAD POR TAB                      │
└──────────────────────────────────────────────────────┘

ESTADO       │ MESAS │ LLEVAR │ COBRAR │ API DEVUELVE
─────────────┼───────┼────────┼────────┼──────────────
'abierta'    │ SÍ*   │ SÍ*    │ NO     │ SÍ
'cerrada'    │ NO    │ NO     │ SÍ     │ SÍ
'cobrada'    │ NO    │ NO     │ NO     │ NO

* Solo aparece en MESAS si mesa_numero != 'PARA_LLEVAR'
* Solo aparece en LLEVAR si mesa_numero = 'PARA_LLEVAR'
```

---

## Flujo de Datos Simplificado

```
BD (verdad)
   │
   ├─ estado
   ├─ mesa_numero
   └─ total_pedidos
        │
        ▼
    API Filter
    (WHERE estado IN (...))
        │
        ▼
    Frontend Receive
    (GET /areas-activas)
        │
        ├─ cuentasAbiertas = filter(estado='abierta')
        ├─ cuentasCerradas = filter(estado='cerrada')
        │
        ├─ cuentasMesa = filter(abierta + mesa_numero != 'LLEVAR')
        ├─ cuentasLlevar = filter(abierta + mesa_numero = 'LLEVAR')
        ├─ cuentasCobrar = filter(cerrada)
        │
        ▼
    Render Tabs
    ├─ Tab "MESAS" = renderizar(cuentasMesa)
    ├─ Tab "LLEVAR" = renderizar(cuentasLlevar)
    └─ Tab "COBRAR" = renderizar(cuentasCobrar)
```

---

## Ejemplo Concreto con Números

**Inicial**: BD vacía

```
Fetch → API devuelve []
Tabs: MESAS=0, LLEVAR=0, COBRAR=0
```

**Después de crear Cuenta 001 (Mesa 2)**:

```
BD: id=1, estado='abierta', mesa_numero=2
Fetch → API devuelve [{id:1, estado:'abierta', mesa_numero:2}]
Tabs:
  - MESAS = [{Cuenta 001}]
  - LLEVAR = []
  - COBRAR = []
```

**Después de crear Cuenta 002 (PARA_LLEVAR)**:

```
BD: id=2, estado='abierta', mesa_numero='PARA_LLEVAR'
Fetch → API devuelve [{id:1, ...}, {id:2, estado:'abierta', mesa_numero:'PARA_LLEVAR'}]
Tabs:
  - MESAS = [{Cuenta 001}]
  - LLEVAR = [{Cuenta 002}]
  - COBRAR = []
```

**Después de cerrar Cuenta 001**:

```
BD: id=1, estado='cerrada', mesa_numero=2
Fetch → API devuelve [{id:1, estado:'cerrada'}, {id:2, ...}]
Tabs:
  - MESAS = []
  - LLEVAR = [{Cuenta 002}]
  - COBRAR = [{Cuenta 001}]
```

**Después de cobrar Cuenta 001**:

```
BD: id=1, estado='cobrada', mesa_numero=2
Fetch → API devuelve [{id:2, ...}]  (Cuenta 001 excluida por estado='cobrada')
Tabs:
  - MESAS = []
  - LLEVAR = [{Cuenta 002}]
  - COBRAR = []
```

---

**CONCLUSIÓN**: El flujo es predecible, lineal y sin ambigüedades ✓
