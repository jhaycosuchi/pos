# 🏗️ Arquitectura Visual - Comanda Digital

## Estructura de Componentes

```
┌─────────────────────────────────────────────────────────────────┐
│                        app/comanda/page.tsx                      │
│                      (Estado Global + Lógica)                    │
│                                                                   │
│  • pedidos: Pedido[]                                             │
│  • itemsCompletados: Set<string>                                │
│  • autoRefresh: boolean                                          │
│  • loading: boolean                                              │
│                                                                   │
│  • cargarPedidos()                                              │
│  • cambiarEstado()                                              │
│  • toggleItemCompletado()                                       │
│  • calcularTiempoTranscurrido()                                 │
│  • getColorPorTiempo()                                          │
└─────────────────────────────────────────────────────────────────┘
           │
           ├─────────────────────────────────┬─────────────────────────┐
           │                                 │                         │
    ┌──────▼──────┐           ┌──────────────▼──────────────┐    ┌────▼─────────┐
    │   loading   │           │    ComandaHeader.tsx        │    │ max-w-7xl    │
    │   state     │           │                             │    │ (Container)  │
    │             │           │  • Auto-refresh toggle      │    │              │
    └─────────────┘           │  • Manual refresh button    │    └──────────────┘
                              └─────────────────────────────┘
                                        │
                  ┌─────────────────────┼─────────────────────┐
                  │                     │                     │
          ┌───────▼────────┐   ┌────────▼────────┐   ┌────────▼────────┐
          │ ComandaColumn  │   │ ComandaColumn   │   │ ComandaColumn   │
          │                │   │                 │   │                 │
          │ PENDIENTES     │   │ EN PREPARACIÓN  │   │ LISTOS          │
          └────────────────┘   └─────────────────┘   └─────────────────┘
                  │                     │                     │
     ┌────────────┴──────────┐  ┌───────┴────────┐  ┌────────┴─────────┐
     │                       │  │                │  │                  │
 ┌───▼──────┐          ┌────▼──▼───┐       ┌────▼──▼────┐    ┌─────────▼──┐
 │ Column   │          │ ColumnHdr │       │ ColumnHdr  │    │ ColumnHdr  │
 │ Header   │          │           │       │            │    │            │
 │          │          │ EN PREP   │       │ LISTOS     │    │            │
 │ PENDIENTES          │ (count)   │       │ (count)    │    └────────────┘
 │ (count)  │          └────────────┘      └────────────┘
 └──────────┘                │                    │
     │                       │                    │
 ┌───▼────────────────┐ ┌────▼──────────┐   ┌────▼──────────┐
 │ Pedido Card x N    │ │ Pedido Card xN │   │ Pedido Card x N│
 │ (sin checkboxes)   │ │ (con checkboxes)  │   │(sin checkboxes)
 │                    │ │                │   │                │
 │ ┌──────────────┐   │ │ ┌────────────┐ │   │ ┌────────────┐ │
 │ │PedidoHeader  │   │ │ │PedidoHeader│ │   │ │PedidoHeader│ │
 │ └──────────────┘   │ │ └────────────┘ │   │ └────────────┘ │
 │                    │ │                │   │                │
 │ ┌──────────────┐   │ │ ┌────────────┐ │   │ ┌────────────┐ │
 │ │PedidoItem[]  │   │ │ │ItemCheckbox│ │   │ │PedidoItem[]│ │
 │ │(solo lectura)│   │ │ │[]  (activos)   │   │ │(con checks)│ │
 │ └──────────────┘   │ │ └────────────┘ │   │ └────────────┘ │
 │                    │ │                │   │                │
 │ ┌──────────────┐   │ │ ┌────────────┐ │   │ ┌────────────┐ │
 │ │ActionButton  │   │ │ │CompletedSec│ │   │ │ActionButton│ │
 │ │"Comenzar"    │   │ │ └────────────┘ │   │ │"Entregado" │ │
 │ │(preparando)  │   │ │                │   │ └────────────┘ │
 │ └──────────────┘   │ │ ┌────────────┐ │   │                │
 │                    │ │ │ItemCheckbox│ │   │ ┌────────────┐ │
 │ ┌──────────────┐   │ │ │[]  (compl) │ │   │ │NoItemsMsg  │ │
 │ │NoItemsMessage│   │ │ └────────────┘ │   │ │(si vacío)  │ │
 │ │(si vacío)    │   │ │                │   │ └────────────┘ │
 │ └──────────────┘   │ │ ┌────────────┐ │   │                │
 │                    │ │ │ActionButton│ │   └────────────────┘
 └────────────────────┘ │ │"Completar" │ │
                        │ │ (listo)    │ │
                        │ └────────────┘ │
                        │                │
                        │ ┌────────────┐ │
                        │ │NoItemsMsg  │ │
                        │ │(si vacío)  │ │
                        │ └────────────┘ │
                        └────────────────┘
```

## Flujo de Datos (Redux-like)

```
┌──────────────────────────────────────────┐
│     USER INTERACTION (Touch/Click)       │
└──────────────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────┐
│  Component Handler (onClick, onChange)   │
└──────────────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────┐
│   Page.tsx Function (cambiarEstado,      │
│   toggleItemCompletado, etc)             │
└──────────────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────┐
│   API Call (/api/pedidos/[id])           │
│   (PUT with new state)                   │
└──────────────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────┐
│   Update State (setPedidos, etc)         │
└──────────────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────┐
│   Re-render Components                   │
│   (Solo lo que cambió)                   │
└──────────────────────────────────────────┘
              │
              ▼
┌──────────────────────────────────────────┐
│   UI Actualizada (Usuario ve cambio)    │
└──────────────────────────────────────────┘
```

## Ciclo de Auto-Refresh

```
┌─────────────────────┐
│  setAutoRefresh     │
│  (toggle button)    │
└──────────┬──────────┘
           │
           ▼
   ┌───────────────┐
   │ autoRefresh?  │
   ├───────────────┤
   │  true / false │
   └───┬───────┬───┘
       │       │
     true    false
       │       │
       ▼       ▼
   setInterval  clearInterval
   (3000ms)     (detener)
       │       │
       └───┬───┘
           │
           ▼
     cargarPedidos()
     (fetch)
           │
           ▼
    setPedidos(data)
           │
           ▼
      Re-render
```

## Transiciones de Estado

```
┌─────────────┐
│  pendiente  │
│  (ROJO)     │
└──────┬──────┘
       │ click "Comenzar"
       ▼
┌─────────────────┐
│  preparando     │
│  (AMARILLO)     │
│  ✓ Con checkboxes
└──────┬──────────┘
       │ click "Completar"
       ▼
┌──────────────┐
│  listo       │
│  (VERDE)     │
└──────┬───────┘
       │ click "Entregado"
       ▼
┌──────────────┐
│  entregado   │
│  (GRIS)      │
│  ✓ Fuera de la comanda
└──────────────┘
```

## Tipos de Datos

```typescript
interface Pedido {
  id: number                              // Identificador único
  mesa_numero: number | null              // Mesa o null si es llevar
  es_para_llevar: number                  // 0/1 (falso/verdadero)
  numero_pedido: string                   // P001, P002, etc
  mesero_nombre: string                   // Juan, María, etc
  total: number                           // $100.00
  estado: string                          // 'pendiente'|'preparando'|'listo'|'entregado'
  creado_en: string                       // ISO 8601 timestamp
  items: DetallePedido[]                  // Array de items
}

interface DetallePedido {
  nombre: string                          // "Pasta Carbonara"
  cantidad: number                        // 2
  especificaciones: string                // "Sin queso"
  notas: string                           // "Urgente"
  es_restriccion: number                  // 0/1 alergia, restricción
}

type EstadoPedido = 'pendiente' | 'preparando' | 'listo' | 'entregado'
```

## Colores Semánticos

```
Tiempo Transcurrido:
├─ Verde (0-15 min):      rgb(22, 163, 74)    ✓ Todo bien
├─ Amarillo (15-30 min):  rgb(202, 138, 4)    ⚠ Revisar
└─ Rojo (>30 min):        rgb(220, 38, 38)    ❌ Urgente

Estados:
├─ Pendiente (Rojo):      border-red-600      alert
├─ Preparando (Amarillo): border-yellow-600   cooking
└─ Listo (Verde):         border-green-600    ready
```

## Performance

```
Carga Inicial:
├─ HTML/JS:     87.6 kB (shared)
├─ Comanda Page: 5 kB (específico)
└─ Total:       92.6 kB first load

Auto-Refresh:
├─ Intervalo:   3000ms (3 segundos)
├─ Request:     GET /api/pedidos (~2KB)
├─ Update:      < 500ms
└─ Impact:      Bajo (solo recarga si cambió)
```

---

**Diagrama actualizado:** 2024
**Componentes:** 10
**Tipos:** Typesafe (TypeScript)
**Status:** ✅ Optimizado
